<% if request.path == '/map'%>
  <%= yield %>
<% else %>
  <div class="container">
    <div class="starter-template">
      <%= render 'layouts/alert' %>
      <%= yield %>
    </div>
  </div>
<% end %>