<div class="comment-box">
  <table id="dataTable-agroecologia" class="table table-striped table-bordered" width="100%" cellspacing="0">
    <thead><tr><th style="display: none;"></th></tr></thead>

    <tbody>
    <% comentarios.each do |comentario| %>
      <tr>
        <td  style="width: 90%; padding-bottom: 32px; padding-top: 32px; text-align: justify;">
          <%= simple_format(comentario.texto) %>
          <div class="comment-footer">By <%= link_to comentario.usuario.nome, usuario_path(comentario.usuario) %> on <%= comentario.created_at %></div>
            <% if usuario_signed_in? %>
              <% if current_usuario.admin? or comentario.usuario.id == current_usuario.id %>
                <div style="text-align: right;">
                  <%= link_to t(:destroy), comentario_path(comentario), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-sm btn-danger' %>
                </div>
              <% end %>
           <% end %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>