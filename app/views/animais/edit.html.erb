<ul class="nav nav-tabs">
  <% if usuario_signed_in? %>
    <li class="nav-item">
      <%= link_to t(:all), animais_path, :class => "nav-link" %>
    </li>
  <% end %>
  <li class="nav-item">
    <%= link_to "#{t(:edit)} #{@animal.nome}", edit_animal_path(@animal), :class => "nav-link active" %>
  </li>
</ul>

<br/>

<%= render 'form', animal: @animal %>
