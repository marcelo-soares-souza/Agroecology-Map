<%= link_to "Registrar Nova Planta", new_planta_path, :class => "btn btn-primary", :style => "float:right;" %>
<br/><br/><br/>

<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to t(:all), plantas_path, :class => "nav-link active" %>
  </li>
</ul>

<% if @plantas.empty? %>
  <br/>
  <h3>Nenhuma Planta Registrada</h3>
<% else %>

  <br/>
  <div style="overflow-x:auto;">
    <table id="dataTable-agroecologia" class="table table-striped table-bordered" width="100%" cellspacing="0">
      <thead>
        <tr>
          <th></th>
          <th>Nome</th>
          <th>Nome Científico</th>
          <th>SAFs</th>
          <% if usuario_signed_in? %><th></th><% end %>
        </tr>
      </thead>

      <tbody>
        <% @plantas.each do |planta| %>
        <tr>
          <td scope="row"><%= link_to image_tag(planta.imagem.url(:thumb), :height => "80"), planta_path(planta) %></td>
          <td><%= link_to planta.nome, planta_path(planta) %></td>
          <td><%= planta.nome_cientifico %></td>
          <td><%= planta.safs.count %></td>
          <% if usuario_signed_in? %>
            <td>
              <% if current_usuario.admin? or planta.usuario.id == current_usuario.id %>
                <%= link_to t(:edit), edit_planta_path(planta), :class => 'btn btn-mini btn-success' %>
                <%= link_to t(:destroy), planta_path(planta), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Você tem Certeza?')) }, :class => 'btn btn-mini btn-danger' %>
              <% end %>
            </td>
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <script>$(document).ready(function(){ dataTables(); });</script>
<% end %>
