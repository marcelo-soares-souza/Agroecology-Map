<% title "Editando #{@planta.nome} | Sistemas Agroflorestais" %>

<ul class="nav nav-tabs">
  <% if usuario_signed_in? %>
    <li class="nav-item">
      <%= link_to t(:all), plantas_path, :class => "nav-link" %>
    </li>
  <% end %>
  <li class="nav-item">
    <%= link_to "#{t(:edit)} #{@planta.nome}", edit_planta_path(@planta), :class => "nav-link active" %>
  </li>
</ul>

<br>

<%= render 'form', planta: @planta %>
