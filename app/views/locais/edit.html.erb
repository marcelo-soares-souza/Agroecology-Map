<% title "Editando #{@local.nome}" %>

<ul class="nav nav-tabs flex-column flex-xl-row">
  <li class="nav-item">
    <%= link_to "Locais", locais_path, :class => "nav-link" %>
  </li>
  <li class="nav-item">
    <%= link_to "#{t(:edit)} #{@local.nome}", edit_local_path(@local), :class => "nav-link active" %>
  </li>
</ul>
<br>
<%= render 'form', local: @local %>
